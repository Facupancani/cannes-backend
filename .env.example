# Copiar este archivo a ".env" y completar los valores para el entorno local.

# Puerto en el que escucha el servidor Express
PORT=3000

# Conexión a la base de datos (MySQL / Sequelize)
DB_NAME=cannes_db
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_DIALECT=mysql
